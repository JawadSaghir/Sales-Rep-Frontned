# Dependencies
node_modules/

# Next.js build output
.next/
out/
# Throwaway build dir for verifying a build without disturbing a running
# `next dev` — see distDir in next.config.js.
.next-verify/

# Env - NEXT_PUBLIC_API_BASE is set in the Vercel dashboard, not committed
.env*
!.env.example

# TypeScript incremental build info
*.tsbuildinfo

# Editor / OS
.DS_Store
.idea
.vscode

# Vercel CLI link data
.vercel

# Incoming design bundles. Source material, already landed into app/, components/
# and lib/ — kept locally for reference but never committed: this directory is
# git-subtree-pushed to the public frontend repo, and these folders are also
# excluded from tsconfig.json (their un-renamed `-repId-` files import a
# lib/data.ts that means something else here, so tsc fails on them).
/Login and admin dashboard redesign/
/nextjs/
INTEGRATION_PROMPT.md
